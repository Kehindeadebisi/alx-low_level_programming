#include <stdio.h>
#include <stdlib.h>
#include "list.h"

/**
 *print_listint - prints list
 *@h: pointer to head
 *Return: number of nodes
 */

size_t print_listint(const listint_t *h)
{
	size_t n = 0;

	while (*h != NULL)
	{
		n++;
		printf ("%i\n", h->next);
	}

	return (n);
}
