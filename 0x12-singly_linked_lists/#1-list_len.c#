#include "lists.h"
#include <stdio.h>

/**
 *list_len - prints number of elements
 *@h: pointer to head
 *Return: number of elements
 */

size_t list_len(const list_t *h);
{
	size_t n = 0;

	if (h != NULL)
	{n = h->len;
		return (n);
	}
	else
	{
		return (1);
	}
}
