#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 *malloc_checked - allocates memory to pointer
 *@b: integer
 *Return: pointer
 */

void *malloc_checked(unsigned int b)
{
	int *ptr = malloc(sizeof(*ptr) * b);

	if (*ptr == '\0')
	{
	exit(98);
	}
	return (0);
}
